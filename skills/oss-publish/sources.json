{
  "created_at": "2026-07-22T00:00:00.000Z",
  "type": "merge",
  "merged_from": ["secure-npm-package", "secure-ruby-gem"],
  "sources": [
    {
      "url": "https://github.com/evilmartians/agent-skills/tree/main/skills/secure-npm-package",
      "repository": "evilmartians/agent-skills",
      "path": "skills/secure-npm-package",
      "branch": "main",
      "sha": "0813c8304717dc2fa392b6f4a475f3ac6c8f7336",
      "original_name": "secure-npm-package",
      "fetched_at": "2026-07-22T00:00:00.000Z",
      "ported_via": "secure-npm-package"
    },
    {
      "url": "https://github.com/evilmartians/agent-skills/tree/main/skills/secure-npm-package",
      "repository": "evilmartians/agent-skills",
      "path": "skills/secure-npm-package",
      "branch": "main",
      "sha": "0813c8304717dc2fa392b6f4a475f3ac6c8f7336",
      "original_name": "secure-npm-package",
      "fetched_at": "2026-07-22T00:00:00.000Z",
      "ported_via": "secure-ruby-gem",
      "note": "secure-ruby-gem's sources.json cites this upstream for structure. Its RubyGems and Sigstore content is original and was reverified against official sources in July 2026."
    }
  ],
  "modifications": [
    "Merged the registry-independent process into SKILL.md and moved npm, RubyGems, PyPI, and crates.io details into references.",
    "Added GitHub Actions and GitLab CI/CD coverage where each registry officially supports them, with explicit gaps for unsupported combinations.",
    "Added tag-to-manifest version checks, exact artifact handoffs, native approval gates, and post-publish provenance verification.",
    "Updated GitHub examples to the current majors, read from each action's own latest release on 2026-07-27: checkout v7, setup-node v7, setup-python v7, upload-artifact v7, download-artifact v8, and actions/attest v4. The three v7 bumps are drop-in for these snippets. checkout and setup-node changed no input they use, and setup-python v7 removed only pip-install, which no snippet sets.",
    "Made each registry's flow publish the exact artifact it built, rather than depending on client behavior. npm stages the tarball produced by npm pack, on an exact Node release resolved from Node's official archive, along with the npm version that release bundles. RubyGems signs and pushes the built gem explicitly. PyPI's GitLab flow generates and uploads PEP 740 attestations explicitly, since plain trusted publishing does not create them there. The crates.io GitLab token exchange fails closed, prints neither token, and installs no package once OIDC is available.",
    "Recorded that crates.io serves no provenance. GitHub attests the exact uploaded .crate independently; GitLab remains below R-PUB-03.",
    "Recorded GitHub and GitLab plan limits for native approval gates and forbade unverified third-party approval substitutes.",
    "The PyPI and crates.io GitLab publish jobs bind environment: name: beside their id_tokens: block, and say what that binding is and is not. Both registries compare only the environment claim's string, crates.io ignores the environment_protected claim GitLab also emits, and neither form binds a ref, so an unprotected environment leaves any pushable branch satisfying every claim the registry checks. Protected environments are GitLab Premium and Ultimate. The pypi.md and crates.md tag-to-version guards are now exact commands rather than angle-bracket placeholders, each carrying what to derive from the repository: PyPI's reads a static project.version and says where to compare instead when the version is dynamic, and crates.io's names the crate for workspaces and uses jq -e so an unmatched name fails the step. Recorded for the crates.io GitLab exchange script: sourcing it leaves set -eu applied to the rest of the script: block, errexit is kept deliberately and nounset is turned off at the end rather than in a subshell the token could not escape, and --fail-with-body needs curl 7.76 or later. references/rubygems.md names the separated build-and-publish workflow as the default and keeps rubygems/release-gem as the escape hatch, with the exposure that comes from running the project's full bundle in the credentialed job stated where it is offered. No claim in the four references carries a timestamp any more; each names the documentation to re-read instead, because a date in a file a reader copies from is a fact that expires without telling anybody. Each tool a reader has to supply is named where its command first appears: pypi-attestations with the install command its own README publishes, gh installed and authenticated for gh attestation verify, and the npm CLI on the maintainer's own machine for the provenance check. The plan limits on native approval gates live in the four references only, because a run reads one of them."
  ],
  "license": "MIT",
  "copyright": [
    "Copyright (c) 2026 Leonid Svyatov",
    "Copyright (c) evilmartians"
  ]
}
